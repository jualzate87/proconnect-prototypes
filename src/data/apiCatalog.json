[
  {
    "id": "client-service",
    "name": "Client Service API",
    "description": "Sync client data with Salesforce, HubSpot, Karbon, or any CRM. Build two-way workflows so contact updates and address changes flow automatically between systems—eliminating duplicate data entry.",
    "category": "Client Management",
    "icon": "👥",
    "useCase": "Build automated onboarding workflows that create clients in ProConnect when they're added to your CRM. Sync address changes instantly across all platforms.",
    "logicFlow": "1. Authenticate with API key → 2. Add or update client data → 3. Sync with external systems → 4. Validate data integrity → 5. Maintain consistency across platforms",
    "endpoints": [
      {
        "method": "GET",
        "path": "/v2/clients",
        "description": "List all clients with optional filtering",
        "parameters": [
          {
            "name": "status",
            "type": "string",
            "required": false,
            "description": "Filter by client status (active, inactive)"
          },
          {
            "name": "lastUpdated",
            "type": "date",
            "required": false,
            "description": "Filter clients updated after specified date"
          }
        ]
      },
      {
        "method": "GET",
        "path": "/v2/clients/{clientId}",
        "description": "Get detailed information about a specific client"
      },
      {
        "method": "POST",
        "path": "/v2/clients",
        "description": "Create a new client record"
      },
      {
        "method": "PUT",
        "path": "/v2/clients/{clientId}",
        "description": "Update existing client information"
      },
      {
        "method": "DELETE",
        "path": "/v2/clients/{clientId}",
        "description": "Archive or delete a client record"
      }
    ]
  },
  {
    "id": "engagement-service",
    "name": "Engagement Service API",
    "description": "Track tax return status, e-file progress, and project milestones in real-time. Integrate with Tableau, Power BI, or project management tools to visualize pipelines and automate status updates—transforming data into actionable insights.",
    "category": "Engagement Data",
    "icon": "📊",
    "useCase": "Create live dashboards showing all returns by status and e-file stage. Trigger automated notifications when engagements reach key milestones or deadlines approach.",
    "logicFlow": "1. Query engagement data → 2. Filter by year, status, or project type → 3. Pull return and plan details → 4. Integrate with BI tools → 5. Automate reporting workflows",
    "endpoints": [
      {
        "method": "GET",
        "path": "/v2/engagements",
        "description": "List all engagements with filtering options",
        "parameters": [
          {
            "name": "year",
            "type": "integer",
            "required": false,
            "description": "Filter by tax year"
          },
          {
            "name": "status",
            "type": "string",
            "required": false,
            "description": "Filter by engagement status (draft, in-progress, complete)"
          },
          {
            "name": "efileStatus",
            "type": "string",
            "required": false,
            "description": "Filter by e-file status (pending, transmitted, accepted, rejected)"
          }
        ]
      },
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}",
        "description": "Get detailed engagement information including returns, projects, and plans"
      },
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}/returns",
        "description": "Get all tax returns associated with an engagement"
      },
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}/projects",
        "description": "Get project details for a specific engagement"
      }
    ]
  },
  {
    "id": "import-service",
    "name": "Import Service API",
    "description": "Pull tax data from payroll systems (ADP, Paychex) or accounting software (QuickBooks, Xero) directly into engagements. Integrate with OCR tools to extract scanned document data and populate returns automatically—cutting data entry time by 80%.",
    "category": "Data Import",
    "icon": "📥",
    "useCase": "Automatically import W-2 data from payroll providers into tax returns. Build integrations that pull prior-year return data and pre-populate current year engagements.",
    "logicFlow": "1. Connect to data source → 2. Map data fields to engagement structure → 3. Validate imported data → 4. Import into ProConnect → 5. Confirm successful import",
    "endpoints": [
      {
        "method": "POST",
        "path": "/v2/engagements/{engagementId}/import",
        "description": "Import data into a specific engagement",
        "parameters": [
          {
            "name": "dataType",
            "type": "string",
            "required": true,
            "description": "Type of data being imported (forms, client-data, prior-year)"
          },
          {
            "name": "source",
            "type": "string",
            "required": false,
            "description": "Source system or file format"
          }
        ]
      },
      {
        "method": "POST",
        "path": "/v2/engagements/{engagementId}/import/validate",
        "description": "Validate import data before committing to engagement"
      },
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}/import/history",
        "description": "Get import history and status for an engagement"
      }
    ]
  },
  {
    "id": "export-service",
    "name": "Export Service API",
    "description": "Export engagement data and calculations to analytics platforms, document management systems, or custom tools. Automatically archive completed returns, push data to advisory planning software, or generate Excel reports—making ProConnect data work everywhere.",
    "category": "Data Export",
    "icon": "📤",
    "useCase": "Export calculated tax data to financial planning software for advisory services. Automatically archive completed returns to SharePoint or document management systems.",
    "logicFlow": "1. Select engagement or data set → 2. Choose export format (JSON, CSV, PDF) → 3. Specify data fields or calculations → 4. Export data → 5. Integrate with downstream systems",
    "endpoints": [
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}/export",
        "description": "Export engagement data and calculated values",
        "parameters": [
          {
            "name": "format",
            "type": "string",
            "required": false,
            "description": "Export format (json, csv, xml, pdf)"
          },
          {
            "name": "includeCalculations",
            "type": "boolean",
            "required": false,
            "description": "Include calculated values in export"
          },
          {
            "name": "dataFields",
            "type": "array",
            "required": false,
            "description": "Specific fields to include in export"
          }
        ]
      },
      {
        "method": "GET",
        "path": "/v2/engagements/{engagementId}/export/forms",
        "description": "Export forms and schedules as PDFs"
      },
      {
        "method": "POST",
        "path": "/v2/engagements/export/bulk",
        "description": "Export data from multiple engagements at once"
      }
    ]
  }
]
